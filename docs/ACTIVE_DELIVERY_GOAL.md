# 持续交付目标与条件

最终状态：本机LLM交互底座的逐项交付核对见`docs/BACKEND_COMPLETION_AUDIT.md`。15条真实完整模型记录、15条当前原生完整长等待、143项交互回归及新增4项profile隔离检查已通过；当前严格预检profile明确区分旧Episode发行认证并实际退出0。旧发行认证不被冒称通过。所有原生复验进程已结束。

账户完成时检查：主窗口resetsAt从1789120269变为1789438372，当前usedPercent=58，两个reset credits仍available，未兑换。按用户窗口重置后停止的条件，不继续数据构造。以下全部为过程历史，不再作为当前未完成项或活跃进程记录；具体边界以交付核对表为准。

最新：原生复验会话54935已正常结束，FAILED=[]，15/15完整时域长等待通过，不再轮询该会话。随后`tools/check_backend_delivery.py --native-dir generated/autonomous_wait_delivery_native_v5`退出0：15条真实模型记录与15条新脚本原生完整轨迹均通过各自独立检查。运行中/7条/11条状态均已过期。143项回归已通过。下一项是最终交付边界核对：旧Episode发布认证与当前LLM交互验收的范围不同，不能将旧包简单改标签或将当前检查冒充旧认证；尚未标记总体完成，未开始数据构造。

组合回归会话34363已结束：143 passed，57.10秒；`git diff --check`通过。原生复验会话54935仍活跃，尚无FDS最终报告，继续轮询同一会话。没有额外付费调用，没有总体完成结论。

最新原生复验仍是会话54935：已确认前7路线（含WNTR）通过，FDS仍在运行。新增联合检查`tools/check_backend_delivery.py --native-dir generated/autonomous_wait_delivery_native_v5`，独立重建脚本微轨迹；当前如实因其余报告未产生而非零退出。新增门禁共18项反例/正例测试通过；旧发布包检查现在会解释具体不一致来源，不再只返回“no output”，没有放宽旧门禁。正在运行143项交互相关组合回归，会话34363，待回收结果。

交付核对新增`docs/BACKEND_DELIVERY.md`和只读`tools/check_backend_delivery.py`：15条记录全部通过其限定范围检查，新增反例测试拒绝缺失/错误来源、旧固定步长、旧前缀证据和不变量失败。当前严格旧发布包preflight仍失败：它绑定改造前源文件；没有更新旧证据或放宽门禁。该失败不能由新记录检查替代。

正在执行当前代码的15路由完整原生长等待复验（零外部API）：进程会话54935，输出`generated/autonomous_wait_delivery_native_v5/`。本轮已确认前六路由通过，正在水系统；必须继续轮询同一会话确认最终状态，不因日志暂时无输出重启。它是脚本执行机制验收，不是真实模型任务成绩。最终交付仍需收敛旧发布入口与当前能力边界，不能宣布完成或开始数据构造。

最新覆盖以`docs/AUTONOMOUS_WAIT_COVERAGE_STATUS.md`为准：15/15正式路由已有真实模型自主等待完整时域的独立交互核对，其中13条自然责任请求、2条明确策略诊断；不能作为15条任务成功。SustainGym诊断中的模型条件选择错误保留。当前转入最终底座交付汇总，尚未开始数据构造。以下覆盖数字和连接阻塞记录为历史，不应作为当前进度。

最新真实等待pilot见 `docs/AUTONOMOUS_WAIT_LLM_PILOT.md`：三条自然任务仍选择单步等待；另一个明确策略诊断在修复路径提示后以2次模型调用推进12个原生步并正确事件唤醒。接口机制证据和模型时间决策能力必须分开，不宣布总体完成。

**最新更正：自主等待已接入主运行器，见 `docs/AUTONOMOUS_WAIT_REPAIR.md`。旧13条固定步长完整结果不等于项目交互验收；原约200MB强制逐步调用计划不继续执行。当前15路由通过本地两微步/单决策检查，100项回归通过，仍待正确协议下的真实模型验收。下面旧覆盖与预算记录仅为历史。**

目标：可用、可信的 LLM 后端（15 路由完整覆盖、真实机制边界、公开观测与接口语义、统一交互、可执行验收）。不是只完成当前已通过的子集。数据构造仅在底座完成后考虑。

用户附加条件：底座完成时若官方额度已经重置，则停止，不继续做数据；若未重置且有额度，再完成数据构造流程。不主动兑换重置券。

本目标开始时读取的额度基线：`codex` 主窗口 10080 分钟，已用 54%，`resetsAt=1789120269`，记录于目标创建后的首次继续运行。当前有两张可用券不等于已经发生重置；未使用它们。完成底座时重新读取额度窗口并与此基线核对。不能仅根据可用额度数判断是否发生官方重置。

## 完成前不可省略

- 15 路由能力、变量时刻/单位、原生/外部限制与不支持项明确且有证据。
- 观测来源与语义一致性，原生未来事件的公开信息隔离，不以关键词扫描代替验证。
- 一条真正逐步调用模型、执行原生动作、保留完整消息/回执与费用、明确错误归属的运行路径。
- 完整时域运行与长会话预算验证；不以两步测试或脚本消息回放替代。
- 可重复执行的分层验收；未完成项保持可见，不能靠删路由或空检查变绿。

当前仍未完成，不能标记 goal complete。最新进展见各状态文档；全仓库历史数据测试缺失不伪装成底座测试通过。

## 当前外部阻塞与恢复入口

最新覆盖：13/15真实完整闭环，含双车96步及FDS6步；55项交互相关回归通过。尚余SustainGym和EnergyPlus IAQ两条长会话。已向用户说明两条保留完整历史预计累计消息约200MB，并请求确认完整运行预算；未在用户答复前启动这一量级的新调用。这个预算选择不等于官方Codex额度重置，也不代表底座完成。

最新覆盖为11/15真实完整闭环（276次调用）；具体路由和剩余四条见 `docs/TRUSTED_SESSION_RUNNER.md`。连接阻塞已解除，下面是恢复过程的历史记录。

**更新（2026-09-10）：模型POST调用已恢复。** HEAD仍返回500，不能仅凭HEAD判断模型不可用。新运行 `generated/closed_loop_recovery_20260910_v1/` 中，D0、D1电池、D3共享通风分别完成12、8、23次真实模型调用并到达声明终态；共43次、157634 total_tokens。三条均完成状态重建验证与后端关闭。以下连接阻塞记录是历史情况，不再作为当前停止理由。尚未覆盖全部15路由的真实完整运行，长会话验收仍待继续，不能宣布底座完成或开始数据构造。

连续多轮对已授权模型接口的无密钥连接复查仍返回 `curl (35) Recv failure: Connection reset by peer`。目前停止自动推进，等待该服务/网络恢复；不更换目的地、不降低TLS安全、不兑换额度重置券。

停止前重新核对：15/15路由有通过的限定原生来源检查；15/15脚本客户端完整原生时域运行通过；三个真实模型闭环报告仍全为 `model_transport_error`，各一次调用尝试，不能作为模型能力结果。下一项决定性交付证据必须来自真实模型完整交互，本地额外回归不能替代它。长会话的真实上下文可用性与费用也必须等待模型连接，不能用字节数冒充token账单。

恢复后先检查同一服务连接，再用新输出目录执行有界真实闭环，不覆盖旧失败证据、不自动重放未确认动作。根据真实返回决定长会话方案并完成最终验收；必要的协议变更需保持用户已确认的信息条件。底座真正完成后才检查官方额度是否重置，并执行用户的数据构造条件分支。当前没有宣布完成，也没有进入数据构造。
